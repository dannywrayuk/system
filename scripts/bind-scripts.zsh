# tmux sessionizer
alias tmux-sessionizer="$DOTFILES/scripts/tmux-sessionizer"
bindkey -s ^p "tmux-sessionizer\n"

# navigator
alias op=". $DOTFILES/scripts/navigator"
bindkey -s ^o "op\n"

# open neovim here
bindkey -s ^n "nvim\n"

alias v="$DOTFILES/scripts/list-info"
